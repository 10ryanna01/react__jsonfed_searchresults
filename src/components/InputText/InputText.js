import React from "react";

const InputText = ({ extraClassName, placeText }) => (
  <div className={extraClassName}>
    <input className="makeaclasee" placeholder={placeText} />
  </div>
);

export default InputText;
